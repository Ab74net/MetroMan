{
  "name": "metro-man",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev:frontend": "npm run dev --workspace @metro/frontend",
    "dev:backend": "npm run dev --workspace @metro/backend",
    "db:migrate": "npm run db:migrate --workspace @metro/backend",
    "db:seed": "npm run db:seed --workspace @metro/backend",
    "db:reset": "npm run db:reset --workspace @metro/backend"
  }
}

